{
  "name": "grymsynth-examples",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node simple-example.js",
    "build": "tsc -b",
    "clean": "rimraf ../dist/examples",
    "prebuild": "npm run clean",
    "prepare": "npm run build",
    "debug": "NODE_ENV=development node --inspect simple-example.js"
  },
  "dependencies": {
    "grymsynth": "file:.."
  },
  "devDependencies": {
    "@types/node": "^18.18.6",
    "rimraf": "^5.0.5",
    "ts-node": "^10.9.1",
    "typescript": "^5.2.2"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
