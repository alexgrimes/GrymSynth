{
  "name": "audio-learning-hub-examples",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "ts-node-esm run-example.ts",
    "build": "tsc -b",
    "clean": "rimraf ../dist/examples",
    "prebuild": "npm run clean",
    "prepare": "npm run build",
    "debug": "NODE_ENV=development ts-node-esm --inspect run-example.ts"
  },
  "dependencies": {
    "../": "file:.."
  },
  "devDependencies": {
    "@types/node": "^18.18.6",
    "rimraf": "^5.0.5",
    "ts-node": "^10.9.1",
    "typescript": "^5.2.2"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}